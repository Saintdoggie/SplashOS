var themes = {
    themeColors: {
        red: {
            main: [255,0,0],
            background: [200,0,0],
            windowTop: [255]
        }
    },
    set: function(color) {
        if (color == "red") {
            main = [this.themeColors.red.main]

        }
    },
}